export const metadata = {
  title: "ContextGPT | Verify Email",
  description: "Verify your email address to activate your ContextGPT account.",
  robots: { index: false, follow: false },
};

export default function VerifyEmailLayout({ children }) {
  return children;
}
