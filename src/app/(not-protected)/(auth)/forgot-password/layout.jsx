export const metadata = {
  title: "ContextGPT | Forgot Password",
  description: "Reset your ContextGPT account password.",
  robots: { index: false, follow: false },
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
