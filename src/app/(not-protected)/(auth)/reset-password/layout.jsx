export const metadata = {
  title: "ContextGPT | Reset Password",
  description: "Set a new password for your ContextGPT account.",
  robots: { index: false, follow: false },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
